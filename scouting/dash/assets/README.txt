Place your FRC field image here (e.g. field_2025.png).

The AutoPathViewer uses normalised 0-1 coordinates, so any field image
with the correct aspect ratio will work. The larger pixel dimension is
treated as width (landscape orientation).

Recommended: use the same field image from the bot_path_drawer package's
example/assets/ folder.
